/*
const users='{"name":"Aaditya", "age":19, "gender":"Male"}';

const user=JSON.parse(users);
console.log(user.name);
//So if you want to pass a string in the form of object(as a data), it's quite possible that the other end won't understand the contents of the string.
//SO JSON.parse(s) helps to convert the string into an object.


const users={
    name:"Aaditya",
    age:19,
    gender:"Male"
}
const user=JSON.stringify(users);
console.log(user);
//So JSON.stringify(obj) helps to convert the object into a string. It's the opposite of JSON.parse()
*/

